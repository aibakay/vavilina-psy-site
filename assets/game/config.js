// Конфигурация мини-игры «В своём ритме».
// Все игровые параметры и ссылки собраны здесь, чтобы их можно было менять
// без правки логики. Значения подобраны под спокойный темп: игрок не обязан
// бежать быстро, задача — сохранять ресурс.

export const CONFIG = {
  // Базовые размеры игрового поля (логические CSS-пиксели).
  view: {
    minHeight: 360,
    maxHeight: 480,
    // Доля высоты поля, которую занимает «пол» снизу.
    groundRatio: 0.16,
  },

  // Движение мира. Персонаж стоит на месте по X, мир едет влево.
  world: {
    baseSpeed: 205, // px/сек в начале
    maxSpeed: 355, // потолок скорости
    speedGrowth: 3.2, // прибавка px/сек за секунду игры (плавный рост)
    // Замедление от бонуса «Дыхание».
    breathingSlowFactor: 0.55,
    // Длительность мягкой остановки, когда ресурс подошёл к нулю.
    // Игра не «убивает» персонажа — мир плавно тормозит до полной остановки.
    stopDuration: 1.4,
    // Через сколько секунд забега напомнить, что закончить можно самому.
    // Без этого аккуратный игрок бежит бесконечно и не доходит до финала.
    longRunHintAfter: 75,
    // Перевод пройденных пикселей в «метры» пути для счёта.
    metersPerPx: 0.03,
  },

  // Физика персонажа.
  // Прыжок задан в долях роста персонажа и в секундах, а не в абсолютных px:
  // иначе на маленьком экране фигура прыгает на две свои высоты, а на большом —
  // едва отрывается от земли. Реальные gravity/jumpVelocity считаются при
  // ресайзе (см. engine._resize) и одинаково ощущаются на любом устройстве.
  player: {
    // Высота силуэта в стойке (логические px). Пересчитывается от высоты поля.
    standHeightRatio: 0.38, // доля игровой высоты
    jumpHeightRatio: 1.15, // апекс прыжка в «ростах» персонажа
    airTime: 0.92, // сек от отрыва до приземления
    slideDuration: 1.0, // сек в подкате — хватает пройти под широким облаком
    // Ускорение падения при подкате в воздухе (в долях обычной гравитации).
    fastFallFactor: 1.9,
    // Положение персонажа от левого края (доля ширины).
    xRatio: 0.2,
    // Неуязвимость после столкновения.
    invulnerableTime: 1.1,
    // Короткое замедление мира после столкновения (мягкий «сбой ритма»).
    hitSlowDuration: 0.6,
    hitSlowFactor: 0.5,
  },

  // Уменьшенные хитбоксы, чтобы игра ощущалась честной.
  hitbox: {
    playerStand: { w: 0.36, h: 0.82, offX: 0.0, offY: 0.0 },
    playerSlide: { w: 0.7, h: 0.5, offX: 0.0, offY: 0.0 },
    obstacle: 0.68, // общий коэффициент сжатия хитбокса препятствий
    bonus: 1.15, // бонусы собирать чуть легче
  },

  // Шкала внутреннего ресурса.
  // Ресурс не утекает сам по себе: спокойный бег его восстанавливает.
  // Тратится он только на столкновениях — «ресурс уходит на удары, а не на
  // само существование».
  resource: {
    start: 100,
    min: 0,
    max: 100,
    collisionDamage: 15,
    regenPerSecond: 1.4, // медленное восстановление при чистом беге
    regenDelay: 2.2, // сек паузы в восстановлении после столкновения
    footholdRestore: 8, // «точка опоры»
    supportRestore: 20, // бонус «Поддержка»
  },

  // Появление объектов. Дистанция считается от текущей скорости,
  // чтобы время реакции игрока оставалось постоянным на любом темпе.
  spawn: {
    minGapPx: 260, // абсолютный минимум дистанции
    minGapSec: 1.35, // минимальное время реакции между препятствиями
    gapRandomSec: 0.9, // случайная добавка (сек)
    bonusLeadPx: 190, // бонус появляется чуть раньше препятствия
    bonusChance: 0.6, // шанс, что в «окне» появится бонус
    // Веса типов бонусов (точка опоры — основная, остальные редкие).
    bonusWeights: { foothold: 74, breathing: 9, boundaries: 9, support: 8 },
    // Длительность редких бонусов.
    breathingDuration: 4.0, // сек замедления
  },

  // Пути к спрайтам (обслуживаются как статические файлы Eleventy).
  sprites: {
    idle: "/assets/game/01_idle.png",
    run: [
      "/assets/game/02_run_1.png",
      "/assets/game/03_run_2.png",
      "/assets/game/04_run_3.png",
    ],
    jump: "/assets/game/05_jump.png",
    slide: "/assets/game/06_slide.png",
    // Исходная высота «стоячих» кадров (px), к ней приравниваем видимую высоту.
    runFrameFps: 12, // кадров бегового цикла в секунду
  },

  // Ссылки для кнопок финального экрана.
  // breathing: пусто → откроется встроенная дыхательная практика внутри игры.
  // consultation: якорь на существующую секцию записи на сайте.
  cta: {
    breathingHref: "",
    consultationHref: "#contacts",
  },

  // Тексты (спокойные формулировки, без «победи/избавься»).
  copy: {
    title: "В своём ритме",
    introText:
      "Не обязательно бежать быстрее. Ресурс восстанавливается сам, когда бежишь спокойно, — а закончить можно в любой момент.",
    start: "Начать",
    retry: "Попробовать ещё раз",
    breathing: "Дыхательная практика",
    consultation: "Узнать о консультации",
    breathingHint: "Выдохни. Можно не спешить",
    supportHint: "Не всё нужно проходить в одиночку",
    longRunHint: "Ты бежишь уже долго. Закончить можно в любой момент",
    // Заголовки финала: остановка по своей воле и остановка «на нуле».
    finishTitleChosen: "Ты остановился сам — это тоже выбор",
    finishTitleDrained: "Иногда остановиться — тоже часть пути",
    // Мягкий вывод по итогам забега (подбирается в engine._finalNote).
    noteChosen: "Остановиться, пока есть силы, — навык, а не слабость.",
    noteManyFootholds: "Ты замечал опоры по дороге. В жизни это работает так же.",
    noteFewFootholds: "Опоры были рядом, но мимо. Иногда их просто трудно заметить.",
    noteHitMany: "Ударов было много. Не всякий темп стоит того, чтобы его держать.",
  },
};
